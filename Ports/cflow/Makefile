include ../../Library/GNU.mk

Title=		GNU cflow
Name=		cflow
Version=	1.4
Site=		$(GnuHome)/$(Name)
Source=		$(GnuMirror)/$(Name)/$(Name)-$(Version).tar.bz2
License=	GPL

define test_hook
$(BinDir)/cflow --version
endef
